apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx-route
  namespace: test-infra
spec:
  parentRefs:
  - name: nginx-gw
  rules:
  - backendRefs:
    - name: nginx-v1
      port: 8080